---

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  jackson:
    default-property-inclusion: NON_NULL
    
message: this is clay

management:
  endpoints:
    web:
      exposure:
        include: "*"

